apiVersion: v1
kind: Service
metadata:
  name: workflow-agent
  namespace: agent-tracing-demo
spec:
  type: ClusterIP
  selector:
    app: workflow-agent
  ports:
    - name: http
      port: 80
      targetPort: 8080
